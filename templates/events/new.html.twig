{% extends 'base.html.twig' %}

{% block title %}New Events{% endblock %}

{% block body %}
    <div class="min-h-screen flex items-center justify-center py-12">
        <div class="w-full max-w-2xl bg-white p-8 rounded-lg shadow">
            <h1 class="text-2xl font-semibold mb-6 text-center">Ajouter des nouveaux événements</h1>

            {{ include('events/_form.html.twig') }}

            <div class="mt-6 text-center">
                <a href="{{ path('app_events_index') }}" class="inline-flex justify-center py-2 px-4 border border-transparent shadow-sm text-sm font-medium rounded-md text-white bg-red-600 hover:bg-red-700">Retour à la liste</a>
            </div>
        </div>
    </div>
{% endblock %}
