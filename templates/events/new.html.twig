{% extends 'base.html.twig' %}

{% block title %}New Events{% endblock %}

{% block body %}
    <h1 class="text-2xl">Ajouter des nouveaux évenements</h1>
    <div class="w-40 flex items-center content-center">
    {{ include('events/_form.html.twig') }}
    </div>

    <a href="{{ path('app_events_index') }}" class="inline-flex justify-center py-2 px-4 border border-transparent shadow-sm text-sm font-medium rounded-md text-white bg-red-600 hover:bg-red-700">back to list</a>
{% endblock %}
